import React from 'react';
import Disk from "../../components/disc/Disk";

const HomePage = () => {
    return (
        <div>
            Hello there
            <Disk />
        </div>
    );
};

export default HomePage;
